[build]
dockerfilePath = "Dockerfile"

[[services]]
name = "api"
startCommand = "bun run src/db/migrate.ts && bun run src/index.ts"

[[services]]
name = "worker"
startCommand = "bun run src/queue/worker.ts"
